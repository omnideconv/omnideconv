% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rectanglepy.R
\name{build_model_rectanglepy}
\alias{build_model_rectanglepy}
\title{Build a Rectangle signature model}
\usage{
build_model_rectanglepy(
  single_cell_object,
  cell_type_annotations,
  bulk_gene_expression = NULL,
  model_path = NULL,
  optimize_cutoffs = TRUE,
  p = 0.015,
  lfc = 1.5,
  n_cpus = NULL,
  gene_expression_threshold = 0.5,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{single_cell_object}{A matrix with the single-cell data. Rows are genes,
columns are cells. Row and column names must be set. Raw counts expected.}

\item{cell_type_annotations}{A vector of cell type labels, one per column of
single_cell_object.}

\item{bulk_gene_expression}{Optional. A matrix of bulk data (rows = genes,
columns = samples). When provided, the signature is restricted to genes
present in the bulk data.}

\item{model_path}{Optional path where the signature pickle file should be
saved. If NULL (default), a temporary file is used. Providing a permanent
path allows the signature to be reused across R sessions by passing the
same path to \code{\link[=deconvolute]{deconvolute()}} or \code{\link[=extract_signature_rectanglepy]{extract_signature_rectanglepy()}}.}

\item{optimize_cutoffs}{Whether to optimize p-value and log-fold-change
cutoffs via grid search. Recommended but slow. Default: TRUE.}

\item{p}{P-value cutoff for DE analysis. Only used if optimize_cutoffs = FALSE.}

\item{lfc}{Log fold-change cutoff for DE analysis. Only used if
optimize_cutoffs = FALSE.}

\item{n_cpus}{Number of CPUs for DE analysis. NULL uses all available.}

\item{gene_expression_threshold}{Minimum fraction of cells that must express
a gene for it to be included in DE analysis. Default: 0.5.}

\item{verbose}{Whether to print progress to the console. Default: FALSE.}

\item{...}{Additional arguments, silently ignored. Allows extra parameters to pass through
the \code{\link[=deconvolute]{deconvolute()}} dispatcher without error.}
}
\value{
Path to a pickle file containing the RectangleSignatureResult.
Pass this path as the \code{signature} argument to \code{\link[=deconvolute]{deconvolute()}} or
\code{\link[=extract_signature_rectanglepy]{extract_signature_rectanglepy()}}.
}
\description{
Builds a hierarchical signature model from single-cell RNA-seq data using
DESeq2-based marker gene selection and optional hierarchical clustering.
The resulting model can be passed to \code{\link[=deconvolute]{deconvolute()}} to avoid rebuilding
the signature for every bulk dataset.
}
